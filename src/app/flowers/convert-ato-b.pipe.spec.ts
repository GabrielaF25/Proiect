import { ConvertAtoBPipe } from './convert-ato-b.pipe';

describe('ConvertAtoBPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertAtoBPipe();
    expect(pipe).toBeTruthy();
  });
});
